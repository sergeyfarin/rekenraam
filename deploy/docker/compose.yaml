services:
    rekenraam:
        build:
            context: ../..
            dockerfile: deploy/docker/Dockerfile
        ports:
            - "16888:16888"
        volumes:
            - rekenraam-data:/app/data

volumes:
    rekenraam-data:
