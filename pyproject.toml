[project]
name = "rekenraam"
version = "0.1.0"
requires-python = "==3.14.*"
dependencies = [
    "rekenraam-api[dev]",
]

[tool.uv.workspace]
members = [
    "apps/api",
]

[tool.uv.sources]
rekenraam-api = { workspace = true, editable = true }
