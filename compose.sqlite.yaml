services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - rekenraam_data:/data
    restart: unless-stopped

volumes:
  rekenraam_data:
