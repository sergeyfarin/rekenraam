# Bruno Collection

Place Bruno API request collections here.

Suggested environments:

- `local`: backend running on `http://localhost:16888`
- `app`: integrated binary or Docker app running on `http://localhost:16888`
- `test`: throwaway database or test server

Keep requests grouped by feature area once API routes exist.
