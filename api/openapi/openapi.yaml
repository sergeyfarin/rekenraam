openapi: 3.1.0
info:
  title: Rekenraam API
  version: 0.0.0
paths:
  /api/v1/health:
    get:
      summary: Health check
      responses:
        "200":
          description: Health response
          content:
            application/json:
              schema:
                type: object
                required:
                  - status
                properties:
                  status:
                    type: string
                    example: ok
